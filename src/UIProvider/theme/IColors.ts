export type TTheme = 'light' | 'dark';

export type IColors = {
    // Themes from react-navigation
    background: string;
    card: string;
    title: string;
    text: string;
    border: string;
    notification: string;

    grid: string;
    primary: string;
};
